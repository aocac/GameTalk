# 更新日志

版本号遵循 `主.次.修订`：功能新增进次版本，修复与打磨进修订号。每个 GitHub Release 页面也有一份对应的说明。

## 未发布

- 暂无

## v0.7.1 — 2026-09-09

一次全量代码核查后的修复版本，同时引入构建唯一标识。

### 修复（客户端）

- 只发图片不打字时点发送没有反应，图片会消失（空文本校验漏了多图字段）
- 消息超过 50 条的房间，首次打开看不到最新一条消息，向上翻页还会各跳一条
- 房主踢一个已经退房的成员，自己的房间会从侧栏消失
- 退出房间后重新加入同一房间，聊天区一直空白
- 断网期间排队的消息，在重连瞬间可能被整批丢弃
- 历史记录一次加载失败后，该会话在本次运行内不再重试
- 在私聊里收到房间消息时不计未读、也不弹通知
- 重连时可能被从私聊拽回房间
- 窗口最小化或隐藏到托盘时，当前会话的消息不计未读、不弹通知
- 切换账号时，上一个账号的请求响应可能写进新会话
- `@张三` 会误把 `@张三丰` 一起提及
- 头像加载失败后永久空白，换了新头像也不显示
- 采集窗「开始共享」可以双击，导致同时起两套采集
- 浮层比主窗口晚加载时，缩放和显示时长会停留在默认值
- 关闭代理后旧的代理设置仍然生效
- 游戏模式启动过程中被关闭，可能留下一个仍在生效的全局快捷键

### 修复（服务端）

- 退房或房间被删除后，旧连接仍可继续发送、撤回消息
- 私聊撤回多图消息后，图片仍能从历史里取到
- 群共享表情不校验图片归属，可以登记别人的图片
- 邀请链接并发兑换时可能超额使用，或直接返回 500
- 非法 id 或错误类型的参数直达数据库，返回 500 而不是 400
- 编辑消息时新增的 @ 不会生效
- 改名后自己发送的消息仍显示旧用户名
- 代理信任头可被公网客户端伪造，用于绕过登录限流
- 邀请码改用加密安全随机数生成
- 房主退房后仍能删除房间、踢人（现在房主只能删房，不能退房）
- 客户端停止读取数据时，服务端发送缓冲会无限增长（现在超过 8MB 断开该连接）
- 「目标不在房间」和「你不在房间」共用同一个错误码，会让客户端误删自己的房间

### 新增

- 构建唯一标识：每次构建生成 `build.<时间戳>.<git 短 sha>`，显示在登录页与设置「关于」页，并写入安装包文件名，用于区分版本号相同的不同构建

### 其他

- 服务端测试 89 例、客户端 26 例；新增两份回归测试文件覆盖以上修复
- 安装包 2.4MB

## v0.7.0 — 2026-09-08

屏幕共享完整化。

- 晚加入房间的成员能收到正在进行的共享，可以按需点「观看」
- 支持多人同时共享，各自独立观看窗口
- 观看端自动解除静音，有声轨时正常出声
- 采集窗在共享开始后自动隐藏；系统采集提示条尝试用 Win32 接口隐藏
- 服务端新增活跃共享快照，成员被踢、房间被删、共享端断线都会正确清理

## v0.6.1 — 2026-08-30

- 点击 Windows 系统通知回到窗口时，自动跳到对应会话（正在输入时不打断）

## v0.6.0 — 2026-08-30

- 云表情包：个人表情跨设备同步
- 群共享表情库：房间成员共同贡献，全群可用

## v0.5.7 — 2026-08-30

- 一轮全量代码审查后的集中修复（30 项，含私聊乐观消息残留、历史加载被短路、@提及跨会话泄漏等）
- 私聊（DM）错误定向携带会话信息，失败时只清理对应会话

## v0.5.4 — 2026-08-30

- 安装包覆盖升级不再强制卸载旧版本
- 修复自定义 NSIS 模板中的版本号与路径占位符

## v0.5.0 — 2026-08-29

- 消息撤回记录操作者，房主代撤时文案区分
- 消息编辑双端同步「已编辑」标记

## v0.4.8 — 2026-08-29

- 消息编辑（房间 + 私聊）
- 好友私聊（DM）：独立消息通道、会话列表、未读角标

## v0.4.6 — 2026-08-29

- 屏幕覆盖开关
- 设置窗口按分类跳转
- 浮层图文消息优先显示文字

## v0.4.0 — 2026-08-29

- @提及（补全、高亮、独立角标）
- 图片消息与灯箱、表情面板
- 成员管理：右键菜单、限时禁言、移出房间
- 好友系统与在线状态
- 房间花名册（含离线成员）

## v0.3.0 — 2026-08-29

- 消息撤回、引用回复、图片粘贴
- 个人资料（头像 / 昵称 / 个性签名）
- 数据库每日备份 + 异地副本

## v0.2.0 — 2026-08-28

- 多房间、邀请码、游标分页历史
- 成员列表、房间右键菜单、删除房间
- 头像上传与云端分发

## v0.1.1 — 2026-08-28

- 游戏模式修复：快捷键录制、浮层窗口加载、托盘与关闭确认

## v0.1.0 — 2026-08-28

首个版本。

- 游戏内快捷输入（全局快捷键 → 输入框 → Enter 发送 → 焦点还给游戏）
- 绝对透明、点击穿透的消息浮层，位置 / 缩放 / 显示时长可调
- 注册登录（argon2 + JWT）、房间群聊、实时消息与提示音
- Docker Compose + Caddy 一键部署，自动 HTTPS/WSS
