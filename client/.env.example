# --- 客户端默认服务器地址（可选） ---
#
# 填了它，打出来的安装包首次启动就指向该地址，玩家不需要手动填写服务器。
# 不填（公开仓库与 CI 的默认状态）则回落到 http://127.0.0.1:8787（本地开发地址）。
#
# 只在生产构建（vite build / tauri build）生效：
# `vite dev` 与 vitest 一律使用本地开发地址，避免开发机上存在 .env.local 时
# 把开发环境与单测指向生产服务器。
#
# 用法（不要把真实地址提交进仓库）：
#   cp .env.example .env.local
#   编辑 .env.local，填入自己的地址后执行 npm run build:full
#
# .env.local 已被 .gitignore 忽略（.env.* / *.local 两条规则命中）。
VITE_DEFAULT_SERVER_URL=
