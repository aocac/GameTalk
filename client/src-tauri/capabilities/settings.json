{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-window",
  "description": "Capabilities for the standalone settings window",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default"
  ]
}
